// questions-intermediate.js — Questions 26–50 (Intermediate)
const intermediateQuestions = [
  {
    id: 26,
    category: "Intermediate",
    question: "What is OOP in PHP?",
    shortAnswer: "OOP (Object-Oriented Programming) ek programming paradigm hai jisme code ko 'objects' ke roop me organize kiya jata hai jo data aur behavior dono ko ek saath rakhte hain.",
    explanation: "OOP ke 4 main pillars hote hain: Encapsulation, Inheritance, Polymorphism, aur Abstraction. PHP 5 se full OOP support aaya aur PHP 8 tak ye bahut mature ho chuka hai. OOP se code reusable, maintainable, aur scalable banta hai, especially large applications (jaise Laravel framework khud OOP principles par based hai) ke liye.",
    example: "Ek `User` class banakar us se multiple `$user1 = new User()`, `$user2 = new User()` objects create kiye ja sakte hain, har ek apni alag properties ke saath.",
    code: `<?php\nclass Car {\n  public $brand;\n  public function drive() {\n    return $this->brand . " is driving";\n  }\n}\n$car = new Car();\n$car->brand = "Tesla";\necho $car->drive();\n?>`,
    output: "Tesla is driving",
    mistakes: "OOP ko sirf 'classes banana' samajh lena — actually OOP ek design philosophy hai jo encapsulation aur relationships (inheritance, composition) par focus karti hai.",
    interviewDefinition: "Object-Oriented Programming (OOP) in PHP is a paradigm that organizes code into objects combining data and behavior, built on the principles of encapsulation, inheritance, polymorphism, and abstraction."
  },
  {
    id: 27,
    category: "Intermediate",
    question: "Class and object",
    shortAnswer: "Class ek blueprint/template hai, aur object us class ka actual instance hota hai jo memory me exist karta hai.",
    explanation: "Class properties (variables) aur methods (functions) define karti hai, lekin khud memory nahi leti jab tak use instantiate na kiya jaye. `new` keyword se class ka object banaya jata hai. Ek hi class se multiple independent objects bana sakte hain, har ek apni state maintain karta hai.",
    example: "`Car` class ek blueprint hai, jabki `$myTesla = new Car()` ek specific real object hai jiski apni properties (color, brand) hongi.",
    code: `<?php\nclass Student {\n  public $name;\n}\n$s1 = new Student();\n$s1->name = "Krishna";\necho $s1->name;\n?>`,
    output: "Krishna",
    mistakes: "Class aur object ko same samajh lena — class sirf definition hai, object uska real, memory me store instance hai.",
    interviewDefinition: "A class is a blueprint that defines properties and methods, while an object is a concrete instance of that class created in memory using the `new` keyword."
  },
  {
    id: 28,
    category: "Intermediate",
    question: "Constructor",
    shortAnswer: "Constructor ek special method hai (`__construct`) jo object create hote hi automatically call hota hai, generally initialization ke liye.",
    explanation: "Constructor ka use property values set karne, dependencies inject karne, ya kisi initial setup ke liye hota hai. PHP me ek class ka sirf ek constructor ho sakta hai, lekin usme optional/default parameters use karke flexible initialization possible hai. Agar constructor define nahi kiya jaye, PHP ek default (empty) constructor use karta hai.",
    example: "Ek `Database` class ka constructor connection setup kar sakta hai jaise hi object banaya jata hai, taaki har baar manually connect na karna pade.",
    code: `<?php\nclass User {\n  public $name;\n  public function __construct($name) {\n    $this->name = $name;\n  }\n}\n$u = new User("Krishna");\necho $u->name;\n?>`,
    output: "Krishna",
    mistakes: "Constructor ke andar heavy logic (jaise API calls) daal dena, jo object creation ko slow aur unpredictable bana deta hai — heavy logic separate methods me rakhna behtar hai.",
    interviewDefinition: "A constructor is a special method, `__construct()`, that is automatically invoked when an object is instantiated, typically used to initialize properties or perform setup tasks."
  },
  {
    id: 29,
    category: "Intermediate",
    question: "Destructor",
    shortAnswer: "Destructor ek special method (`__destruct`) hai jo object destroy hone se pehle automatically call hota hai, cleanup ke liye.",
    explanation: "Destructor tab call hota hai jab object par koi reference nahi bacha, ya script end ho rahi ho. Ye resources release karne ke liye useful hota hai, jaise file handles close karna ya database connections band karna. PHP me garbage collection ki wajah se destructor ka exact timing predict karna kabhi-kabhi mushkil hota hai.",
    example: "Ek `FileHandler` class ka destructor automatically file close kar sakta hai jab object use me nahi rehta.",
    code: `<?php\nclass Logger {\n  public function __destruct() {\n    echo "Logger destroyed";\n  }\n}\n$log = new Logger();\nunset($log);\n?>`,
    output: "Logger destroyed",
    mistakes: "Critical cleanup logic (jaise transaction commit) sirf destructor par depend karke chhod dena — script ke abnormal termination (fatal error) par destructor guaranteed call nahi hota.",
    interviewDefinition: "A destructor is a special method, `__destruct()`, automatically called when an object is about to be destroyed, commonly used to release resources such as file handles or connections."
  },
  {
    id: 30,
    category: "Intermediate",
    question: "Inheritance",
    shortAnswer: "Inheritance ek mechanism hai jisse ek class (child) dusri class (parent) ki properties aur methods reuse kar sakti hai.",
    explanation: "PHP me `extends` keyword se inheritance implement hoti hai. Child class parent ke sabhi public/protected members ko access kar sakti hai aur naye members add ya override bhi kar sakti hai. PHP single inheritance support karta hai (ek class sirf ek parent extend kar sakti hai), lekin interfaces ke through multiple 'contracts' implement kiye ja sakte hain.",
    example: "`Animal` ek parent class ho sakti hai, aur `Dog`, `Cat` uski child classes jo common behavior (jaise `eat()`) inherit karti hain.",
    code: `<?php\nclass Animal {\n  public function eat() { return "Eating..."; }\n}\nclass Dog extends Animal {\n  public function bark() { return "Barking..."; }\n}\n$d = new Dog();\necho $d->eat() . " " . $d->bark();\n?>`,
    output: "Eating... Barking...",
    mistakes: "Deep inheritance chains (parent-child-grandchild-...) banana jo code ko rigid aur samajhna mushkil bana deta hai — composition often better hoti hai.",
    interviewDefinition: "Inheritance is an object-oriented mechanism allowing a child class to acquire and extend the properties and methods of a parent class using the `extends` keyword."
  },
  {
    id: 31,
    category: "Intermediate",
    question: "Encapsulation",
    shortAnswer: "Encapsulation data aur uske related methods ko ek unit (class) me bundle karta hai, aur internal details ko bahar se hide karta hai.",
    explanation: "PHP me access modifiers (`private`, `protected`, `public`) se encapsulation implement hoti hai. Private properties/methods sirf usi class ke andar accessible hote hain, jisse external code us data ko directly modify nahi kar sakta — data access sirf defined public methods (getters/setters) ke through hota hai, jo validation aur control allow karta hai.",
    example: "Ek `BankAccount` class ka `$balance` property private hona chahiye, aur `deposit()`/`withdraw()` methods se hi ise modify karne diya jana chahiye.",
    code: `<?php\nclass BankAccount {\n  private $balance = 0;\n  public function deposit($amount) {\n    $this->balance += $amount;\n  }\n  public function getBalance() {\n    return $this->balance;\n  }\n}\n$acc = new BankAccount();\n$acc->deposit(500);\necho $acc->getBalance();\n?>`,
    output: "500",
    mistakes: "Sabhi properties ko `public` rakh dena convenience ke liye — isse data integrity aur validation control kho jata hai.",
    interviewDefinition: "Encapsulation is the OOP principle of bundling data and behavior within a class while restricting direct external access using access modifiers like private and protected."
  },
  {
    id: 32,
    category: "Intermediate",
    question: "Abstraction",
    shortAnswer: "Abstraction complex implementation details ko hide karke sirf zaroori features expose karta hai, PHP me abstract classes/methods se implement hoti hai.",
    explanation: "Abstract class direct instantiate nahi ki ja sakti — ye sirf ek base define karti hai jise child classes ko implement karna hota hai. Abstract methods sirf method signature define karte hain, body child class me likhi jati hai. Ye ensure karta hai ki sabhi child classes ek consistent interface follow karein, bina implementation details expose kiye.",
    example: "Ek `PaymentGateway` abstract class me `processPayment()` abstract method ho sakta hai, jise `StripeGateway`, `PaypalGateway` alag-alag tarike se implement karte hain.",
    code: `<?php\nabstract class Shape {\n  abstract public function area();\n}\nclass Circle extends Shape {\n  private $radius;\n  public function __construct($r) { $this->radius = $r; }\n  public function area() { return 3.14 * $this->radius ** 2; }\n}\n$c = new Circle(5);\necho $c->area();\n?>`,
    output: "78.5",
    mistakes: "Abstract class ko directly instantiate karne ki koshish karna (`new Shape()`), jo PHP me fatal error deta hai — abstract classes sirf extend karne ke liye hoti hain.",
    interviewDefinition: "Abstraction is the OOP principle of exposing only essential features while hiding implementation complexity, implemented in PHP through abstract classes and abstract methods."
  },
  {
    id: 33,
    category: "Intermediate",
    question: "Polymorphism",
    shortAnswer: "Polymorphism ka matlab hai ki ek hi method call, different objects ke liye different behavior de sakta hai.",
    explanation: "PHP me polymorphism mainly method overriding ke through implement hota hai — child classes parent ke method ko apne hisaab se redefine karti hain, aur jab us method ko call kiya jata hai, PHP runtime pe decide karta hai ki actual object kaunsi class ka hai aur uska version call karta hai. Ye code ko flexible aur extensible banata hai.",
    example: "Ek `Shape` interface implement karne wali `Circle` aur `Square` classes dono ka `area()` method hoga, lekin har ek ka calculation logic alag hoga.",
    code: `<?php\nclass Animal {\n  public function sound() { return "Some sound"; }\n}\nclass Dog extends Animal {\n  public function sound() { return "Bark"; }\n}\nclass Cat extends Animal {\n  public function sound() { return "Meow"; }\n}\nforeach ([new Dog(), new Cat()] as $animal) {\n  echo $animal->sound() . " ";\n}\n?>`,
    output: "Bark Meow",
    mistakes: "Polymorphism ko function overloading (jo PHP natively support nahi karta) samajh lena — PHP me polymorphism mainly method overriding aur interfaces se achieve hota hai.",
    interviewDefinition: "Polymorphism is the OOP principle that allows objects of different classes to be treated through a common interface, with each class providing its own implementation of shared method calls."
  },
  {
    id: 34,
    category: "Intermediate",
    question: "Interface",
    shortAnswer: "Interface ek contract hai jo methods ke naam define karta hai jinhe implementing class ko zaroor implement karna hota hai, bina koi implementation body ke.",
    explanation: "PHP me `interface` keyword se interface banaya jata hai, aur `implements` keyword se class use adopt karti hai. Ek class multiple interfaces implement kar sakti hai (jo multiple inheritance jaisi flexibility deta hai), lekin sirf ek class extend kar sakti hai. Interfaces me sabhi methods public aur abstract hote hain, aur properties allowed nahi hoti (constants allowed hain).",
    example: "`Countable`, `Iterator` jaise built-in PHP interfaces classes ko standard behavior follow karne par force karte hain.",
    code: `<?php\ninterface Payable {\n  public function pay($amount);\n}\nclass CreditCard implements Payable {\n  public function pay($amount) {\n    return "Paid $amount via Credit Card";\n  }\n}\n$cc = new CreditCard();\necho $cc->pay(100);\n?>`,
    output: "Paid 100 via Credit Card",
    mistakes: "Interface ke andar method ki body likhne ki koshish karna — interfaces sirf method signatures define karte hain, implementation nahi.",
    interviewDefinition: "An interface defines a contract of method signatures that an implementing class must fulfill, enabling multiple, unrelated classes to guarantee consistent behavior."
  },
  {
    id: 35,
    category: "Intermediate",
    question: "Trait",
    shortAnswer: "Trait ek mechanism hai jisse PHP me methods ko multiple classes ke beech reuse kiya ja sakta hai, single inheritance ki limitation ko bypass karke.",
    explanation: "PHP single inheritance follow karta hai, lekin traits se hum ek jaisa reusable code alag-alag unrelated classes me `use` keyword ke through inject kar sakte hain — bina inheritance hierarchy banaye. Agar multiple traits me same method naam ho to conflict resolve karne ke liye `insteadof` aur `as` keywords use hote hain.",
    example: "Ek `Loggable` trait multiple classes (jaise `Order`, `User`) me common `log()` method add kar sakta hai bina unhe ek common parent class se inherit kiye.",
    code: `<?php\ntrait Greetable {\n  public function greet() {\n    return "Hello from " . get_class($this);\n  }\n}\nclass Robot {\n  use Greetable;\n}\n$r = new Robot();\necho $r->greet();\n?>`,
    output: "Hello from Robot",
    mistakes: "Traits ka overuse karna jitna ki 'god trait' bana dena jisme unrelated methods bhare ho — traits ko bhi single-responsibility follow karna chahiye.",
    interviewDefinition: "A trait is a mechanism for horizontal code reuse in PHP, allowing methods to be shared across multiple unrelated classes without relying on class inheritance."
  },
  {
    id: 36,
    category: "Intermediate",
    question: "Static keyword",
    shortAnswer: "`static` keyword se property ya method class-level par belong karta hai, na ki kisi specific object instance ke.",
    explanation: "Static members ko object banaye bina hi `ClassName::$property` ya `ClassName::method()` syntax se access kiya ja sakta hai. Static properties sabhi objects ke beech shared hoti hain — agar ek object usse change kare, to change sabhi jagah reflect hoga kyunki ye ek hi memory location share karte hain, instance-specific nahi hote.",
    example: "Ek `Counter` class me static property se track kiya ja sakta hai ki total kitne objects create hue hain.",
    code: `<?php\nclass Counter {\n  public static $count = 0;\n  public function __construct() {\n    self::$count++;\n  }\n}\nnew Counter(); new Counter();\necho Counter::$count;\n?>`,
    output: "2",
    mistakes: "Static properties/methods ka overuse karna jab actual requirement instance-based behavior ki ho — static state test karna bhi mushkil hota hai (global-state-like issues).",
    interviewDefinition: "The `static` keyword designates a property or method as belonging to the class itself rather than to individual instances, accessible via the class name without instantiation."
  },
  {
    id: 37,
    category: "Intermediate",
    question: "Access modifiers",
    shortAnswer: "Access modifiers (`public`, `protected`, `private`) define karte hain ki class ke members ko kahan se access kiya ja sakta hai.",
    explanation: "`public` members class ke andar, child classes, aur bahar se — kahin se bhi accessible hote hain. `protected` members sirf usi class aur uski child classes ke andar accessible hote hain, bahar se nahi. `private` members sirf usi class ke andar accessible hote hain, child classes me bhi nahi. Ye encapsulation ka core mechanism hai.",
    example: "Ek `Employee` class me `$salary` ko `private` rakhna chahiye taaki bahar se directly modify na ho sake, sirf defined methods se hi access diya jaye.",
    code: `<?php\nclass Employee {\n  private $salary = 50000;\n  protected $department = "IT";\n  public $name = "Krishna";\n}\n$e = new Employee();\necho $e->name; // works\n// echo $e->salary; // Fatal error\n?>`,
    output: "Krishna",
    mistakes: "`protected` aur `private` ko same samajh lena — `protected` child classes ko access allow karta hai, `private` bilkul nahi.",
    interviewDefinition: "Access modifiers—public, protected, and private—control the visibility of class members, determining whether they can be accessed externally, from subclasses, or only within the declaring class."
  },
  {
    id: 38,
    category: "Intermediate",
    question: "Method overriding",
    shortAnswer: "Method overriding tab hota hai jab child class parent class ke kisi method ko apni khud ki implementation se replace kar deti hai.",
    explanation: "Overriding ke liye child class me parent jaisa hi method naam aur signature use karna hota hai. Child ke andar se parent ka original method `parent::methodName()` se bhi call kiya ja sakta hai agar zaroorat ho. Ye runtime polymorphism ka core hai — jo object actually instantiate hua hai, uska method version call hota hai.",
    example: "Ek `Notification` parent class ka `send()` method, `EmailNotification` aur `SmsNotification` child classes me alag tarike se override kiya ja sakta hai.",
    code: `<?php\nclass Shape {\n  public function draw() { return "Drawing a shape"; }\n}\nclass Square extends Shape {\n  public function draw() {\n    return parent::draw() . " - specifically a square";\n  }\n}\necho (new Square())->draw();\n?>`,
    output: "Drawing a shape - specifically a square",
    mistakes: "Overriding karte waqt parent method ka signature (parameters) match na karna, jisse strict types ke sath compatibility errors aa sakti hain.",
    interviewDefinition: "Method overriding occurs when a child class provides its own implementation of a method already defined in its parent class, enabling runtime polymorphism."
  },
  {
    id: 39,
    category: "Intermediate",
    question: "Method overloading concept",
    shortAnswer: "PHP traditional method overloading (same naam, different parameters) support nahi karta, lekin `__call()` aur `__callStatic()` magic methods se similar behavior simulate kiya ja sakta hai.",
    explanation: "Java/C++ jaisi languages me multiple methods same naam ke different signatures ke saath define ki ja sakti hain — PHP me ye directly possible nahi hai. Instead, PHP 'magic methods' provide karta hai: `__call()` jab kisi undefined/inaccessible instance method ko call kiya jaye, aur `__callStatic()` static context ke liye. Inke andar hum arguments count/type check karke apna khud ka dynamic dispatch bana sakte hain.",
    example: "Ek `Calculator` class me `__call()` use karke `add()` method ko 2 ya 3 arguments ke saath alag tarike se handle kiya ja sakta hai.",
    code: `<?php\nclass Calculator {\n  public function __call($name, $args) {\n    if ($name == "add") {\n      return array_sum($args);\n    }\n  }\n}\n$c = new Calculator();\necho $c->add(1, 2, 3);\n?>`,
    output: "6",
    mistakes: "Ye assume karna ki PHP me same naam ke saath alag parameter types wale multiple methods directly define kiye ja sakte hain — PHP fatal 'Cannot redeclare' error dega.",
    interviewDefinition: "PHP does not natively support traditional method overloading; instead, dynamic behavior based on method name and arguments is achieved through the `__call()` and `__callStatic()` magic methods."
  },
  {
    id: 40,
    category: "Intermediate",
    question: "Exception handling",
    shortAnswer: "Exception handling ek mechanism hai jisse runtime errors ko gracefully catch aur handle kiya ja sakta hai, program ko crash hone se bachate hue.",
    explanation: "PHP me `try`, `catch`, aur `finally` blocks use hote hain. `try` block me wo code likha jata hai jo error throw kar sakta hai, `catch` block us error (exception object) ko catch karke handle karta hai, aur `finally` block hamesha execute hota hai chahe exception aaye ya na aaye — cleanup code ke liye useful. Custom exceptions banane ke liye `Exception` class ko extend kiya ja sakta hai.",
    example: "File read karte waqt agar file exist nahi karti, to `throw new Exception(\"File not found\")` se error handle kiya ja sakta hai bina application crash kiye.",
    code: `<?php\ntry {\n  $result = 10 / 0;\n  if (!is_finite($result)) throw new Exception("Division error");\n} catch (Exception $e) {\n  echo "Caught: " . $e->getMessage();\n} finally {\n  echo " | Cleanup done";\n}\n?>`,
    output: "Caught: Division error | Cleanup done",
    mistakes: "Bahut broad `catch (Exception $e)` use karke sabhi types ki exceptions ko generic tarike se handle karna, jisse specific error handling aur debugging mushkil ho jati hai.",
    interviewDefinition: "Exception handling is a control structure using `try`, `catch`, and `finally` blocks to gracefully manage runtime errors, preventing abrupt script termination."
  },
  {
    id: 41,
    category: "Intermediate",
    question: "try/catch/finally",
    shortAnswer: "`try` block risky code rakhta hai, `catch` block exception handle karta hai, aur `finally` block hamesha run hota hai chahe exception aaye ya na aaye.",
    explanation: "Multiple `catch` blocks use karke different exception types ko differently handle kiya ja sakta hai — PHP top-to-bottom check karta hai ki kaunsa catch block matching exception type handle kar sakta hai. `finally` block resource cleanup (jaise database connection close karna, file handle release karna) ke liye perfect hai kyunki ye guaranteed execute hota hai.",
    example: "Database query execute karte waqt `try` me query chalao, `catch` me specific `PDOException` handle karo, aur `finally` me connection close karo.",
    code: `<?php\nfunction divide($a, $b) {\n  try {\n    if ($b === 0) throw new InvalidArgumentException("Cannot divide by zero");\n    return $a / $b;\n  } catch (InvalidArgumentException $e) {\n    return "Error: " . $e->getMessage();\n  } finally {\n    echo "Operation attempted. ";\n  }\n}\necho divide(10, 0);\n?>`,
    output: "Operation attempted. Error: Cannot divide by zero",
    mistakes: "`finally` block ke andar `return` statement use karna jo `try`/`catch` ke return value ko silently override kar deta hai — ye ek subtle aur confusing bug source hai.",
    interviewDefinition: "The `try`/`catch`/`finally` construct allows risky code to be executed within `try`, errors to be handled in `catch`, and cleanup logic to run unconditionally in `finally`."
  },
  {
    id: 42,
    category: "Intermediate",
    question: "Namespaces",
    shortAnswer: "Namespaces class, function, aur constant names ko logically group karte hain, jisse naming conflicts avoid hote hain especially large projects ya libraries me.",
    explanation: "Bina namespaces ke, agar do libraries me same class naam ho (jaise dono me `Logger` class), to PHP conflict throw karega. Namespace `namespace` keyword se define hota hai file ke top par, aur `use` keyword se dusre namespace ki classes ko import kiya jata hai. Composer aur PSR-4 autoloading standard ke saath namespaces ka use bahut common hai modern PHP applications me.",
    example: "Laravel framework me `App\\Models\\User` aur `App\\Http\\Controllers\\UserController` jaise namespaces se code organize hota hai.",
    code: `<?php\nnamespace App\\Services;\nclass PaymentService {\n  public function process() {\n    return "Processing payment";\n  }\n}\n// Usage: (new \\App\\Services\\PaymentService())->process();\necho (new PaymentService())->process();\n?>`,
    output: "Processing payment",
    mistakes: "Namespace declaration file ke sabse pehle statement ke roop me na likhna (comments ya kuch aur code ke baad) — PHP me `namespace` file ki sabse pehli statement honi chahiye.",
    interviewDefinition: "Namespaces provide a way to encapsulate classes, functions, and constants under a logical grouping, preventing naming collisions across large codebases or third-party libraries."
  },
  {
    id: 43,
    category: "Intermediate",
    question: "Composer",
    shortAnswer: "Composer PHP ka dependency manager hai jo project ke required libraries (packages) ko manage, install, aur autoload karta hai.",
    explanation: "Composer `composer.json` file me project ki dependencies define karta hai aur `composer.lock` file exact versions lock karti hai taaki team ke sabhi members same versions use karein. `composer install` command dependencies ko `vendor/` folder me download karta hai. Composer PSR-4 autoloading standard ke through classes ko automatically load karta hai bina manual `require` likhe.",
    example: "Laravel project banate waqt `composer create-project laravel/laravel myapp` command Composer ke through poora framework aur uski dependencies install kar deta hai.",
    code: `<?php\n// composer.json snippet\n// { "require": { "monolog/monolog": "^3.0" } }\nrequire 'vendor/autoload.php';\n// use Monolog\\Logger; ...\necho "Composer autoload ready";\n?>`,
    output: "Composer autoload ready",
    mistakes: "`vendor/` folder ko version control (Git) me commit kar dena — best practice ye hai ki sirf `composer.json` aur `composer.lock` commit ho, `vendor/` `.gitignore` me rahe.",
    interviewDefinition: "Composer is PHP's dependency manager, used to declare, install, update, and autoload third-party libraries and project dependencies via a `composer.json` manifest."
  },
  {
    id: 44,
    category: "Intermediate",
    question: "Autoloading",
    shortAnswer: "Autoloading ek mechanism hai jisse class files ko automatically load kiya jata hai jab pehli baar use ki jaati hain, bina manual `require`/`include` likhe.",
    explanation: "Modern PHP me autoloading generally Composer ke PSR-4 standard se implement hoti hai, jisme namespace structure directly folder structure ko map karti hai. `spl_autoload_register()` function bhi custom autoloading logic define karne ke liye use kiya ja sakta hai. Isse hundreds of classes wale large projects me manual includes ka bojh khatam ho jata hai.",
    example: "`App\\Models\\User` namespace wali class automatically `app/Models/User.php` file se load ho jati hai jab Composer autoloader use karte hain.",
    code: `<?php\nspl_autoload_register(function ($class) {\n  $file = str_replace('\\\\', '/', $class) . '.php';\n  if (file_exists($file)) require $file;\n});\n// new App\\Models\\User(); would auto-load User.php\necho "Autoloader registered";\n?>`,
    output: "Autoloader registered",
    mistakes: "Autoloading ke bawajood galat namespace-to-folder mapping rakhna (PSR-4 rules follow na karna), jisse 'Class not found' errors aati hain.",
    interviewDefinition: "Autoloading is a mechanism that automatically loads class files on demand when they are first referenced, most commonly implemented via Composer's PSR-4 standard, eliminating manual include statements."
  },
  {
    id: 45,
    category: "Intermediate",
    question: "JSON handling in PHP",
    shortAnswer: "PHP `json_encode()` se PHP arrays/objects ko JSON string me convert karta hai, aur `json_decode()` se JSON string ko wapas PHP data structure me convert karta hai.",
    explanation: "JSON (JavaScript Object Notation) ek lightweight data-interchange format hai jo APIs me commonly use hota hai. `json_decode($json, true)` associative array return karta hai, jabki `true` ke bina ye stdClass object return karta hai. Nested data (arrays ke andar arrays/objects) bhi easily handle ho jata hai. Invalid JSON decode karne par `json_last_error()` se error check kiya ja sakta hai.",
    example: "Ek REST API response ko frontend ko bhejne se pehle PHP array ko `json_encode()` se JSON string me convert kiya jata hai.",
    code: `<?php\n$data = ["name" => "Krishna", "role" => "Developer"];\n$json = json_encode($data);\necho $json;\n$decoded = json_decode($json, true);\necho $decoded['name'];\n?>`,
    output: '{"name":"Krishna","role":"Developer"}\nKrishna',
    mistakes: "`json_decode()` ka second parameter (`true` for associative array) dena bhool jana, jisse result stdClass object aata hai aur array-style access (`$data['key']`) fail ho jata hai.",
    interviewDefinition: "PHP handles JSON through `json_encode()`, which converts PHP data structures to JSON strings, and `json_decode()`, which parses JSON strings back into PHP arrays or objects."
  },
  {
    id: 46,
    category: "Intermediate",
    question: "Date/time handling",
    shortAnswer: "PHP me `date()` function aur `DateTime` class ka use dates aur times ko format, manipulate, aur compare karne ke liye hota hai.",
    explanation: "`date()` ek simple function-based approach hai formatting ke liye, jabki `DateTime` class (object-oriented) zyada powerful hai — isse dates add/subtract kiye ja sakte hain (`modify()`, `add()`, `sub()`), timezone handle kiya ja sakta hai, aur do dates ke beech difference (`diff()`) nikala ja sakta hai. `DateTimeImmutable` version bhi hai jo original object ko modify nahi karta, naya object return karta hai.",
    example: "Ek blog post ki 'kitne din pehle publish hui' feature `DateTime` diff() se easily calculate ki ja sakti hai.",
    code: `<?php\n$date = new DateTime("2026-01-01");\n$date->modify("+30 days");\necho $date->format("Y-m-d");\n?>`,
    output: "2026-01-31",
    mistakes: "Timezone set kiye bina dates compare karna, jisse server aur user ke locations alag hone par galat results aa sakte hain — hamesha explicit timezone set karna chahiye.",
    interviewDefinition: "PHP provides date and time handling through the `date()` function for simple formatting and the object-oriented `DateTime`/`DateTimeImmutable` classes for more advanced manipulation, comparison, and timezone-aware operations."
  },
  {
    id: 47,
    category: "Intermediate",
    question: "String functions",
    shortAnswer: "PHP me built-in string functions data ko manipulate, search, aur format karne ke liye use hote hain, jaise `strlen()`, `str_replace()`, `substr()`, `explode()`.",
    explanation: "String functions bahut commonly use hote hain data validation, formatting, aur transformation ke liye. `trim()` extra whitespace remove karta hai, `strtolower()`/`strtoupper()` case change karte hain, `explode()`/`implode()` string ko array me convert/wapas convert karte hain, aur `sprintf()` formatted strings banata hai. Multi-byte strings (jaise UTF-8, Hindi text) ke liye `mb_*` prefixed functions use karna zaroori hota hai.",
    example: "User se aya hua naam `trim()` aur `ucfirst()` se clean karke properly capitalize kiya ja sakta hai display ke liye.",
    code: `<?php\n$str = "  hello world  ";\necho trim($str) . "\\n";\necho strtoupper(trim($str)) . "\\n";\necho str_replace("world", "PHP", trim($str));\n?>`,
    output: "hello world\nHELLO WORLD\nhello PHP",
    mistakes: "Multi-byte (UTF-8) strings par normal `strlen()`/`substr()` use karna instead of `mb_strlen()`/`mb_substr()`, jisse non-ASCII characters (jaise Hindi text) ke saath incorrect results aate hain.",
    interviewDefinition: "PHP offers a rich set of built-in string functions for tasks such as trimming, searching, replacing, splitting, and formatting text, with multi-byte (`mb_*`) variants required for correct handling of non-ASCII character sets."
  },
  {
    id: 48,
    category: "Intermediate",
    question: "Array functions",
    shortAnswer: "PHP me numerous array functions data ko filter, map, sort, aur transform karne ke liye available hain, jaise `array_map()`, `array_filter()`, `array_reduce()`, `sort()`.",
    explanation: "`array_map()` har element par ek function apply karke naya array return karta hai. `array_filter()` condition ke basis par elements ko filter karta hai. `array_reduce()` array ko ek single value me reduce karta hai (jaise total sum). Sorting functions jaise `sort()`, `usort()`, `ksort()` alag-alag tarike se array ko order karte hain (value-based, key-based, ya custom logic based).",
    example: "Ek products array se sirf 'in stock' products filter karke unki prices ko `array_map()` se tax ke saath adjust kiya ja sakta hai.",
    code: `<?php\n$nums = [1, 2, 3, 4, 5];\n$doubled = array_map(fn($n) => $n * 2, $nums);\n$even = array_filter($nums, fn($n) => $n % 2 === 0);\n$sum = array_reduce($nums, fn($carry, $n) => $carry + $n, 0);\nprint_r($doubled);\nprint_r($even);\necho $sum;\n?>`,
    output: "Array\n(\n    [0] => 2\n    [1] => 4\n    [2] => 6\n    [3] => 8\n    [4] => 10\n)\nArray\n(\n    [1] => 2\n    [3] => 4\n)\n15",
    mistakes: "`array_filter()` ke baad array keys reset karna bhool jana — filtering ke baad keys ke beech 'gaps' rah jate hain, jo `array_values()` se fix kiye ja sakte hain.",
    interviewDefinition: "PHP provides a rich set of array functions—such as `array_map()`, `array_filter()`, `array_reduce()`, and various sort functions—for transforming, filtering, aggregating, and ordering array data."
  },
  {
    id: 49,
    category: "Intermediate",
    question: "Anonymous functions",
    shortAnswer: "Anonymous functions (ya lambda functions) aise functions hain jinka koi defined naam nahi hota, ye variables me assign kiye ja sakte hain ya directly kisi function ko argument ke roop me pass kiye ja sakte hain.",
    explanation: "Anonymous functions `function() {...}` syntax se banaye jate hain aur inhe callback ke roop me use karna bahut common hai, jaise `array_map()`, `usort()` jaise functions ke saath. PHP 7.4 se `fn() => expression` syntax wali 'arrow functions' bhi aayi hain jo automatically parent scope ke variables ko capture kar leti hain (bina `use` keyword ke).",
    example: "Ek button click handler (JavaScript jaisa concept) PHP me kisi array processing callback ke roop me anonymous function se implement kiya ja sakta hai.",
    code: `<?php\n$multiply = function($a, $b) {\n  return $a * $b;\n};\necho $multiply(4, 5);\n?>`,
    output: "20",
    mistakes: "Anonymous function ke andar parent scope ka variable use karne ki koshish karna bina `use()` keyword ke (regular closures me) — ye 'Undefined variable' error deta hai.",
    interviewDefinition: "Anonymous functions are unnamed functions that can be assigned to variables or passed as arguments, commonly used as callbacks for functions like `array_map()`, `usort()`, and event handlers."
  },
  {
    id: 50,
    category: "Intermediate",
    question: "Closures",
    shortAnswer: "Closure ek anonymous function hai jo apne surrounding (parent) scope ke variables ko 'capture' aur remember kar sakta hai, `use` keyword ke through.",
    explanation: "By default, anonymous function apne outer scope ke variables ko access nahi kar sakta jab tak explicitly `use ($variable)` na likha jaye. Variables by-value (copy) ya by-reference (`use (&$variable)`) capture kiye ja sakte hain — reference se capture karne par outer variable me changes closure ke andar bhi reflect hote hain aur vice versa.",
    example: "Ek counter generator function closure return kar sakta hai jo har call par apna internal count remember rakhe aur increment kare.",
    code: `<?php\nfunction createCounter() {\n  $count = 0;\n  return function() use (&$count) {\n    return ++$count;\n  };\n}\n$counter = createCounter();\necho $counter();\necho $counter();\necho $counter();\n?>`,
    output: "123",
    mistakes: "By-value capture (`use ($count)` bina `&` ke) use karke expect karna ki har call par value persist/update hogi — bina reference ke har call par variable ki ek fresh copy use hoti hai, wo persist nahi hoti.",
    interviewDefinition: "A closure is an anonymous function that captures variables from its enclosing scope—by value or by reference using the `use` keyword—allowing it to retain state across invocations."
  }
];
